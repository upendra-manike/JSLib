export const name = '@rn-debug/release-kit';
export const description = 'Release-mode diagnostics and crash breadcrumb tooling.';

export function summarize(): string {
  return `${name}: ${description}`;
}
