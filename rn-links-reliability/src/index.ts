export const name = '@rn-links/reliability';
export const description = 'Deep link validation harness and routing fallbacks.';

export function summarize(): string {
  return `${name}: ${description}`;
}
