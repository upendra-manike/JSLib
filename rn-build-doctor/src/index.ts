export const name = '@rn-build/doctor';
export const description = 'CLI to detect and resolve native dependency conflicts in RN projects.';

export function summarize(): string {
  return `${name}: ${description}`;
}
