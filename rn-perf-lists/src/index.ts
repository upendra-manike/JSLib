export const name = '@rn-perf/lists';
export const description = 'High-performance list rendering helpers for FlatList and Recycler views.';

export function summarize(): string {
  return `${name}: ${description}`;
}
