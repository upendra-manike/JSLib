export const name = '@rn-perf/bootstrap';
export const description = 'Runtime bootstrap utilities tuned for RN startup performance.';

export function summarize(): string {
  return `${name}: ${description}`;
}
