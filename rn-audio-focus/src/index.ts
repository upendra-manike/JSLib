export const name = '@rn-audio/focus';
export const description = 'Audio focus management and background playback helpers.';

export function summarize(): string {
  return `${name}: ${description}`;
}
