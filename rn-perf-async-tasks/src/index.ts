export const name = '@rn-perf/async-tasks';
export const description = 'JSI worker pool and async task orchestration for heavy RN jobs.';

export function summarize(): string {
  return `${name}: ${description}`;
}
