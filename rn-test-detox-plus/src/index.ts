export const name = '@rn-test/detox-plus';
export const description = 'Stable Detox harness utilities and waitFor presets.';

export function summarize(): string {
  return `${name}: ${description}`;
}
