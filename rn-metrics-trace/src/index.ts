export const name = '@rn-metrics/trace';
export const description = 'Performance tracing and frame-drop monitoring abstractions.';

export function summarize(): string {
  return `${name}: ${description}`;
}
