export const name = '@rn-nav/sentinel';
export const description = 'Navigation diagnostics and persistence guards for complex stacks.';

export function summarize(): string {
  return `${name}: ${description}`;
}
