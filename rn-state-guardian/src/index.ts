export const name = '@rn-state/guardian';
export const description = 'State container health monitors and persistence middlewares.';

export function summarize(): string {
  return `${name}: ${description}`;
}
