export const name = '@rn-net/realtime';
export const description = 'Reliable realtime socket/watchdog utilities with reconnection logic.';

export function summarize(): string {
  return `${name}: ${description}`;
}
