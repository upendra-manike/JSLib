export const name = '@rn-net/secure-storage';
export const description = 'Transactional secure storage helpers with env awareness.';

export function summarize(): string {
  return `${name}: ${description}`;
}
