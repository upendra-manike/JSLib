export const name = '@rn-media/pipeline';
export const description = 'Media handling pipeline for compression, uploads, and caching.';

export function summarize(): string {
  return `${name}: ${description}`;
}
