export const name = '@rn-ui/responsive';
export const description = 'Responsive layout primitives, scaling, and keyboard-safe hooks.';

export function summarize(): string {
  return `${name}: ${description}`;
}
