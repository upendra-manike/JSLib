export const name = '@rn-sensors/perms';
export const description = 'Unified sensor permission flows and capability detection.';

export function summarize(): string {
  return `${name}: ${description}`;
}
