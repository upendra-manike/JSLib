export const name = '@rn-a11y/inspector';
export const description = 'Accessibility audit runners and screen reader tooling.';

export function summarize(): string {
  return `${name}: ${description}`;
}
