export const name = '@rn-net/offline-sync';
export const description = 'Offline-first sync engines with conflict resolution utilities.';

export function summarize(): string {
  return `${name}: ${description}`;
}
