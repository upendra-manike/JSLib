export const name = '@rn-build/ota-safe';
export const description = 'OTA compatibility checks and rollback helpers for RN apps.';

export function summarize(): string {
  return `${name}: ${description}`;
}
