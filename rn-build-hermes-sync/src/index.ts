export const name = '@rn-build/hermes-sync';
export const description = 'Hermes/RN version alignment and toggling utilities.';

export function summarize(): string {
  return `${name}: ${description}`;
}
